FROM node:18-alpine

WORKDIR /app

# package.json과 package-lock.json 복사
COPY frontend/package*.json ./

# 의존성 설치
RUN npm install

# 소스 코드 복사
COPY frontend/ .

# 개발 서버 실행을 위한 설정
EXPOSE 3000

# 개발 모드로 실행 (빌드 없이)
CMD ["npm", "start"]
